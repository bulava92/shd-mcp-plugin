# Controller observability widget

This artifact is served by the SHD MCP backend as `ui://shd/controller-observability/v1.html`.

It is a standalone parameterized MCP App. The same UI contract renders controller history, series comparison, expert settings, lightweight topology, telemetry health and source audit modes; source inventory is loaded separately through the bounded paginated inventory request.
